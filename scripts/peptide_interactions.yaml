# Curated peptide drug interactions.
#
# Rules of the road:
#   - Every row MUST carry either a `source_url` (PubMed, DrugBank ID, or
#     manufacturer/regulator page) OR set `source_kind: no_data`. No silent
#     fakery — if we can't cite it, the row is omitted and the backend's
#     no_data synthesizer covers the pair.
#   - Pair order doesn't matter; the ingest script canonicalises (min,max).
#   - Severity values: major | moderate | minor | unknown.
#
# If a pair is missing here AND openFDA has nothing, the backend will return
# a synthesized no_data row at request time.

pairs: []
